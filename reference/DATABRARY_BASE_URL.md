# Load Package-wide Constants into Local Environment

Load Package-wide Constants into Local Environment

## Usage

``` r
DATABRARY_BASE_URL
```

## Format

An object of class `character` of length 1.
